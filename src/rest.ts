const sendInvite = (friend1:string, friend2: string, friend3:string) => {
  console.log(`send invite ${friend1}`)
  console.log(`send invite ${friend2}`)
  console.log(`send invite ${friend3}`)
}

sendInvite("shakhawat", "hossain", "arman")